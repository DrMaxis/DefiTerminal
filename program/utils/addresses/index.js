const {mainnet} = require("./mainnet");
const {kovan} = require("./kovan");
const {ropsten} = require("./ropsten");


module.exports = {
    mainnet: mainnet,
    kovan: kovan,
    ropsten: ropsten,
};
